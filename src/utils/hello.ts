/**
 * Fonction qui retourne un message de salutation personnalisé
 * @param name - Le nom de la personne à saluer
 * @returns Un message de salutation au format "hello world {name}"
 */
export function helloWorld(name: string): string {
  return `hello world ${name}`;
}
